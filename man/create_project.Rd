% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_registry.R
\name{create_project}
\alias{create_project}
\title{Create a new consulting project}
\usage{
create_project(
  project_name,
  term = NULL,
  start_date = NULL,
  category = NA,
  contact,
  department,
  organization = NA,
  status = "intake",
  consultants = Sys.info()[["user"]],
  notes = NA
)
}
\arguments{
\item{project_name}{Project name (title).}

\item{term}{Academic term label (e.g., \code{"2026SP"}). If \code{NULL},
the term is inferred from the system date.}

\item{start_date}{Optional project start date. Defaults to \code{Sys.Date()}.}

\item{category}{Optional category label (e.g., \code{"Education"}).}

\item{contact}{Primary contact name or email. Used to form the project ID.}

\item{department}{Department name. Used to form the project ID.}

\item{organization}{Optional organization name.}

\item{status}{Initial project status. Defaults to \code{"intake"}.}

\item{consultants}{Character vector of consultant names. Defaults to the current system user.}

\item{notes}{String of any additional notes about the project.}
}
\value{
Invisibly returns a named list containing:
\itemize{
\item \code{project_id}
\item \code{project_path} (relative to the Box root)
\item \code{registry_file} (full path to the JSON file)
}
}
\description{
Creates a new consulting project by:
\itemize{
\item prompting the user to select the project folder (relative to the SCC Box root),
\item generating a project ID of the form \code{term_department_contact},
\item writing a canonical JSON project record to the project registry.
}
}
\details{
The project registry is stored at \code{<Box Root>/project_registry/},
with one JSON file per project named \code{<project_id>.json}.
}
\examples{
\dontrun{
create_project(
  project_name = "Student retention analysis",
  consultants = c("Jane Doe", "A. Smith"),
  category = "Education",
  department = "Student Success",
  contact = "jdoe@example.edu",
  organization = "Example University"
)
}

}
